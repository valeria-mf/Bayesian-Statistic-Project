Questa cartella contiene una versione del codice che runna su CLion senza usare R.

Appena risolveremo alcuni problemi riscontrati su R sposteremo nei file di progetto principali tutto quello che c'è qui ma manca lì.

(Per ora dovrebbe trattarsi solo del calcolo del log[P(X,Z)] e della funzione ausiliaria compute_cardinality)




IMPORTANTE: Modificare il CMakeList in modo che 

include_directories(...) 

abbia la directory in cui è locato "eigen-3.4.0" 